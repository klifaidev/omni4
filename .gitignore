# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Electron build output
release/

# Variáveis de ambiente — nunca versionar (podem conter chaves)
.env
.env.*

# Supabase (recurso de colaboração removido; só resta cache local da CLI)
supabase/

# Dumps/backups locais de dados do app — contêm dados reais de negócio
recovery-localstorage/

# Configurações locais do Claude Code (caminhos da máquina)
.claude/settings.local.json
.claude/launch.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
